const { Service } = require('egg');
class articleService extends Service {
    // async list() {
    //     return await this.app.mysql.get('articles');
    // }
}
module.exports = articleService;
